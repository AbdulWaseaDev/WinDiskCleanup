# WinDiskCleanup — Claude Code Instructions

## Commit Policy

Never add `Co-Authored-By` trailers to commit messages. Commit solely under the git user identity already configured (`AbdulWaseaDev`), with no attribution lines for Claude or any AI tool.
